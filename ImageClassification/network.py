# We used resnet18 as our model so we didn't define it here
import torch.nn as nn

class Network(nn.Module):
    def __init__(self):
        super(Network, self).__init__()
        # define the network layers

    def forward(self, x):
        # define the network structure
